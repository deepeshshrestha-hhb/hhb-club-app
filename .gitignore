# Secrets - never commit
.env
.env.*
!.env.example

# Member PII - regenerated from Spond on app startup, do not version
data/hhb_members.csv

# Player profile runtime data - stored in R2, not source-controlled
data/player_profiles.json
data/profile_photos/

# Photo metadata - created on first upload, stored in R2, not source-controlled
data/Photos.xlsx

# Uploaded images - stored in R2, downloaded to local cache at startup
static/images/photos/
static/images/podium/

# Local dev log
flask_dev.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code — local/personal config, not project state
.claude/settings.local.json
.claude/launch.json
